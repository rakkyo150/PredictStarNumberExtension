{
  "name": "predict_star_number_extension",
  "version": "1.4.1",
  "scripts": {
    "cargo-install": "cargo install --locked cargo-about wasm-pack",
    "wasm-pack": "wasm-pack build --target web --out-dir pkg",
    "build:prod": "npm run wasm-pack && vite build --mode production --config vite.config.beatsaver.ts && vite build --mode production --config vite.config.scoresaber.ts && vite build --mode production --config vite.config.background.ts && npm run license",
    "build:dev": "vite build --mode develop --config vite.config.beatsaver.ts && vite build --mode develop --config vite.config.scoresaber.ts && vite build --mode develop --config vite.config.background.ts",
    "preview": "vite preview",
    "license": "cargo about init && cargo about generate about.hbs -o release/DEPENDENCY_LICENSE.html"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.12",
    "typescript": "~5.8.3",
    "vite": "^7.1.7",
    "vite-plugin-top-level-await": "^1.6.0",
    "vite-plugin-wasm": "^3.5.0"
  }
}
